#include <iostream>
#include <cstring>
using namespace std;

int main(){
   const char* str = "Line";
   int length = strlen(str);
   cout << "the lenght of the line: "<< length << endl;
   return 0;
}